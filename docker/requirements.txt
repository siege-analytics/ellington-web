black
boto3
Django
django-cors-headers
siege-utilities[geo]>=3.17.2
django-extensions
django-grappelli
djangorestframework
djangorestframework-gis
django-postgres-copy
whitenoise
celery[redis]
flower
apache-sedona[spark,pandas,geopandas]
fiona
geopandas
geopy
daphne
gunicorn
httpie
ipdb
pathlib
matplotlib
pandas
psycopg2-binary
pycairo
pysal
python-dotenv
requests
reportlab
reportlab[pycairo]
reportlab[accel]
reportlab[renderpm]
reportlab[pycairo]
scikit-learn
pyRealParser==0.1.0
music21>=9.9.0,<10
# omr-leadsheet — Phase 4-PDF Python OMR pipeline (#70/#81).
# Installed from GitHub until 1.0.0 lands on PyPI. The Python deps
# (music21, pillow, matplotlib, typer) come in via this; the system
# deps (Audiveris, MuseScore, Tesseract, Poppler, ImageMagick) are
# the worker image's responsibility, not pip's.
#
# The dheerajchand/omr-leadsheet repo IS the canonical upstream — it
# is not a fork. (Plugin-agent #85 review NIT assumed a
# siege-analytics/omr-leadsheet existed; it doesn't.) If we ever
# split a siege-analytics fork for org-owned releases, bump this
# URL then.
omr-leadsheet @ git+https://github.com/dheerajchand/omr-leadsheet.git@ced92abc0226e5d7ad4a539e1607e2c1ad2c959f
